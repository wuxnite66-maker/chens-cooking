import { Splash } from "@/components/Splash";

/**
 * Splash / language gate — cinematic animated entry (see components/Splash).
 * Backdrop: real photo of the terrace, with curtain intro, letter cascade,
 * embers, parallax & glowing country buttons.
 */
export default function SplashPage() {
  return <Splash />;
}
